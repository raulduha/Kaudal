# TASKS — Tablero de construcción de Kaudal (para Claude Code)

> **Cómo trabajar (LEER PRIMERO, Claude Code):**
> 1. Toma la **primera tarea sin marcar** `[ ]` de arriba hacia abajo (respeta el orden; no saltes fases).
> 2. Antes de codear, lee el/los doc(s) de `docs/eng/` que indica la tarea.
> 3. Haz el trabajo en una rama/PR. NO hardcodees colores: usa `brand/brand.config.ts`.
> 4. Corre los **subagentes** que indica la tarea (ej. `security-auditor`, `diseno-moderno`).
> 5. Verifica la **Definición de Hecho (DoD)** de la tarea.
> 6. **Marca la casilla** `[x]` y anota una línea en el "Registro" del final (qué hiciste).
> 7. Si te bloqueas 2-3 veces, detente y pide contexto en vez de insistir.
>
> Regla de oro: seguro por defecto (secretos server-side, aislamiento por cliente, API keys cifradas) y visual/simple para el usuario.

> ⚠️ **MODELO (leer antes de construir):** Los **agentes son flujos de n8n** que el operador arma; **Kaudal es el PANEL** que los registra, despliega (Railway), muestra su uso, cobra (Flow) y da tickets — NO es el motor de agentes. El demo Mastra en `app/src/mastra/` es solo ejemplo de arranque, no el motor. Construye el PANEL. Detalle en `CLAUDE.md` (sección MODELO DE AGENTES), `docs/18` y `docs/eng/00`.

Estado del proyecto: **Ruta 1 (Next.js + Mastra) lista en `app/`.** Falta todo lo demás.

---

## Fase 0 · Base (hecho)
- [x] Scaffold Next.js 15 + Mastra en `app/` (agente demo + `/api/run`) — compila y corre.
- [x] Docs de ingeniería `docs/eng/00..09` y definición `docs/18`.
- [x] Subagentes en `.claude/agents/` y marca en `brand/brand.config.ts`.

## Fase 1 · Design system
- [x] **1.1** Configurar Tailwind en `app/` con tokens de `brand/brand.config.ts` (oscuro por defecto).  → subagentes: `diseno-moderno`
- [x] **1.2** Componentes base (tarjeta de agente, chip de estado, KPI card, tarjeta de cliente, tarjeta de ticket, badge de rol, inputs, botones, toasts) según `docs/07` y `docs/eng/05`.  → `diseno-moderno`, `accesibilidad`
- [x] **1.3** Layouts por rol: Operador (acento naranjo) y Cliente (acento menta), con badge visible.
- [x] DoD Fase 1: la app sigue corriendo, se ven los componentes en una página de muestra, `diseno-moderno` y `accesibilidad` sin hallazgos altos.

## Fase 2 · Datos y seguridad base
- [x] **2.1** Conectar Supabase; crear tablas de `docs/eng/02` (orgs, clientes, usuarios, api_keys_cifradas, agentes, registros_uso, suscripciones, cobros, tickets, mensajes_ticket, audit_log).  → `db-guardian`
- [x] **2.2** Habilitar RLS con aislamiento por org/cliente; `audit_log` append-only.  → `db-guardian`, `security-auditor`
- [x] **2.3** Prueba automatizada: un cliente NO puede leer datos de otro.  → `qa-tester`
- [x] DoD Fase 2: RLS probada; `security-auditor` sin hallazgos altos.

## Fase 3 · Auth y roles
- [x] **3.1** Auth Supabase (cookie HTTP-only, revalidación server-side) según `docs/eng/03`.  → `security-auditor`
  - ⚠️ **Pendiente al desplegar a un proyecto Supabase cloud real**: replicar ahí `[auth] enable_signup = false` (Dashboard → Authentication → Sign In/Providers → "Allow new users to sign up" = off). `supabase/config.toml` solo gobierna la instancia LOCAL; el cloud no lo lee. Sin esto, cualquiera con la `anon key` (pública por diseño) podría auto-registrarse. **No** tocar `[auth.email].enable_signup` para lograrlo — ver Registro 3.1: ese flag apaga el login completo, no solo el signup.
- [x] **3.2** Roles operador/cliente; rutas protegidas por rol; el cliente NO se auto-registra.
- [x] DoD Fase 3: login funciona por ambos lados; `code-reviewer` ok.

## Fase 4 · Inscribir cliente (operador)
- [x] **4.1** Pantalla + API para inscribir cliente (razón social, RUT, contacto) y crear su cuenta (`docs/eng/00` flujo 3.1, `docs/eng/05`).  → `diseno-moderno`, `code-reviewer`
- [x] **4.2** Invitación / primer ingreso del cliente (define contraseña).
- [x] DoD Fase 4: el operador crea un cliente y ese cliente puede entrar.

## Fase 5 · API key del cliente (CIFRADA) — CRÍTICO
- [x] **5.1** Flujo donde el cliente ingresa su API key; cifrado server-side (AES-GCM/libsodium o KMS); nunca texto plano, nunca al frontend, nunca en logs (`docs/eng/03`).  → `security-auditor` (bloqueante), `compliance-cl`
  - ✅ ~~Tope de altas por cliente/hora dentro del RPC~~ — resuelto 2026-08-27, ver Registro.
  - ⚠️ **Obligatorio antes de producción** (`security-auditor` lo marcó explícitamente "antes de producción, no antes del merge"): cabeceras de seguridad (CSP sobre todo, para la pantalla donde se pega la key) en `next.config.mjs`, hoy vacío salvo `reactStrictMode`.
- [x] **5.2** Mostrar solo enmascarada (sk-…AB12) + estado válida/inválida; operador solo ve enmascarada (`docs/18` §9).
- [x] DoD Fase 5: `security-auditor` sin hallazgos altos; la key nunca aparece en claro en red/logs.

## Fase 6 · Registrar agente que ya corre
- [x] **6.1** Alta de agente por endpoint/webhook + healthcheck visual (`docs/eng/01`, `docs/eng/05`).  → `diseno-moderno`, `code-reviewer`
- [x] **6.2** Guardar modelo que usa, canal y estado (vivo/caído).
- [x] **6.3** (nuevo, hallazgo transversal de `security-auditor` en 6.1) Escribir en `audit_log` las acciones sensibles del sistema — hoy la tabla existe, es append-only, tiene RLS/índices probados desde Fase 2, pero **ningún** Route Handler de la app inserta en ella todavía (ni inscribir cliente, ni conectar API key, ni registrar agente). Viola la regla de oro 5 de `CLAUDE.md` ("Todo auditable"). No es específico de esta fase — es hueco desde la 2.1 — así que no se cierra parcialmente acá.  → `db-guardian`, `docs-writer`
- [x] DoD Fase 6: registrar un agente y ver su estado.

## Fase 7 · Uso y costo ("dónde se usa")
- [x] **7.1** Endpoint para que el agente reporte uso + estimación por usos×modelo (`docs/eng/07`).
  - ⚠️ **Obligatorio antes de producción** (`security-auditor`): proxy de borde que reescriba `X-Forwarded-For` real (si no, el rate-limit por IP se evade rotando el header); mover el rate-limit a Redis si corre más de una instancia; decidir si `Idempotency-Key` debe ser obligatoria cuando `metodoReporte='reportado'` (hoy es opcional, un agente reportado que la omite puede duplicar costo en un reintento); agregar rotación/revocación de `ingest_token` sin tener que borrar el agente (hoy la única palanca es pausar/archivar).
- [x] **7.2** Integrar la lógica de `tools/calculadora-agentes.html` como pantalla.
- [x] **7.3** Pantalla del cliente "Dónde se usa": uso por día/agente, costo estimado, modelo, y límite con aviso al 80% (`docs/eng/06`, `docs/18` §10). Tiempo real (WebSocket).  → `diseno-moderno`, `accesibilidad`
  - ⚠️ **Alcance reducido a propósito**: "Tiempo real (WebSocket)" NO se construyó — no existe infraestructura de tiempo real en el proyecto todavía (ni WebSocket propio ni Supabase Realtime configurado/auditado). La pantalla se actualiza al navegar/recargar (`router.refresh()` tras guardar el límite), no en vivo. Construir tiempo real de verdad es una tarea propia — evaluar Supabase Realtime (ya viene con el stack, evita un servidor WS a mano) vía suscripción a `registros_uso`/`uso_diario` filtrada por RLS, con su propia auditoría de seguridad (es un canal de datos nuevo).
- [x] DoD Fase 7: el cliente ve su uso y su límite con aviso.

## Fase 8 · Portal del cliente (visual, simple)
- [x] **8.1** Inicio del cliente + "Mi agente" (caja negra: modelo, estado, cómo le va — sin exponer prompts/lógica) (`docs/eng/06`).  → `diseno-moderno`, `accesibilidad` (bloqueante)
- [x] **8.2** Microcopy y estados vacíos que enseñan; cero jerga.
- [x] DoD Fase 8: portal bonito y entendible sin manual.

## Fase 9 · Dudas y reclamos
- [x] **9.1** Cliente crea tickets (duda/reclamo) con adjunto (`docs/eng/08`).  → `security-auditor` (adjuntos/permisos)
- [ ] **9.2** Bandeja kanban del operador + hilo de chat para responder; estados; notificación en vivo.  → `diseno-moderno`, `qa-tester`
- DoD Fase 9: cliente pregunta, operador responde, cliente ve la respuesta.

## Fase 10 · Cobros (Flow + boleta/factura DTE)
- [ ] **10.1** Suscripción Flow (crear, cargo, webhook con validación de firma) (`docs/eng/07`, `docs/13`).  → `security-auditor`
- [ ] **10.2** Emisión de boleta/factura vía proveedor DTE al confirmarse el pago; CLP + IVA.  → `compliance-cl`
- [ ] **10.3** Pantalla de cobros (operador) + estado de cuenta (cliente).  → `code-reviewer`
- DoD Fase 10: se genera un cobro y se emite documento tributario.

## Fase 11 · Despliegue
- [x] **11.1** Dockerfile + variables de entorno (llaves nunca en código) (`docs/eng/09`, `docs/16`).  → `devops-infra`
- [ ] **11.2** Deploy a Railway + CI (build/lint/typecheck/tests) con gates de `security-auditor` y `db-guardian`.  → `deployment`
  - ⚠️ **Ojo al armar el CI** (hallazgo de `security-auditor` en 6.1): `npm run typecheck` da falso verde con la caché incremental de `tsconfig.tsbuildinfo` — encontró un error real de sintaxis en `src/lib/crypto/bytea.ts` que solo aparecía con la caché borrada. El pipeline de CI tiene que correr `typecheck` en un checkout limpio (sin `tsconfig.tsbuildinfo` de una corrida anterior) o con `--incremental false`, si no la DoD "typecheck limpio" no es confiable.
- DoD Fase 11: la app corre en Railway.

## Fase 11.5 · Auto-despliegue y costos (nunca perder plata en una instancia)
- [x] **11.5.1** Tabla `instancias` + campos de suscripción (cubre_instancia, margen) y RLS (`docs/eng/10` §5).  → `db-guardian`
- [x] **11.5.2** Registrar instancia desplegada a mano + gating: no queda activa sin suscripción Flow activa (`docs/eng/10` §2 regla 1).  → `code-reviewer`
- [x] **11.5.3** Auto-suspensión por impago: webhook Flow -> suspende/reactiva; periodo de gracia; el cliente ve solo estado vivo/suspendido (`docs/eng/10` §2 regla 3, §6).  → `security-auditor` (sandbox)
- [x] **11.5.4** Chequeo de rentabilidad: mantención ≥ costo instancia + margen, con la calculadora (`docs/eng/10` §8).
- [ ] **11.5.5** (Opcional, después) Automatizar el botón "Desplegar" con la Railway API (clonar n8n + Postgres, inyectar variables) (`docs/eng/10` §4).  → `devops-infra`, `security-auditor`
- DoD Fase 11.5: ninguna instancia corre sin pago; impago la suspende sola; cada cliente cubre su costo.

## Fase 12 · Endurecimiento final (antes del primer cliente real)
- [ ] **12.1** Pasada de `security-auditor` (foco API keys), `compliance-cl`, `qa-tester` (aislamiento entre clientes), `accesibilidad`, `diseno-moderno`.
- [x] **12.2** Documentar con `docs-writer`; reporte GO/NO-GO.
- DoD Fase 12: GO para poner el primer cliente real.

---

## 🎯 Hito final
- [ ] Un cliente real: inscrito por el operador, con su **API key cifrada**, viendo su **uso**, con un **reclamo respondido**, y **cobrado por Flow con boleta emitida**.

---

## Registro (Claude Code anota acá qué hizo, 1 línea por tarea)

- 2026-08-30 · 12.2 · Reporte GO/NO-GO de sandbox creado en `docs/eng/13-reporte-go-no-go-2026-08-30.md`; deja evidencia local y bloqueadores reales de producción, sin falsear el estado de Flow, DTE, Railway, Realtime o accesibilidad manual.

- 2026-08-30 · 11.5.3 · Sandbox de morosidad: webhook firmado guarda cinco días de gracia; endpoint interno HMAC suspende instancias vencidas y pago las reactiva. El portal usa `instancias_publicas` y solo muestra estado. Railway y el cron del host siguen siendo NO-GO de producción.
- (ej: 2026-08-26 · 1.1 · Tailwind + tokens configurados, page de muestra ok)
- 2026-08-25 · 1.1 · Tailwind v3 instalado en `app/`, `tailwind.config.ts` importa colores/tipografía/radios directo de `brand/brand.config.ts` (sin hardcodear); página de muestra en `/design`; `typecheck` y `build` ok. `diseno-moderno` sin hallazgos altos; pendientes menores para 1.2: cargar fuente Geist/Inter real, mapear `statusColors` para el chip de estado, y ocultar `/design` antes de producción.
- 2026-08-26 · 1.2 · 9 componentes base en `app/src/components/ui/` (Button, Input/Textarea/Select, Badge/RoleBadge, StatusChip/AgentStatusChip, KeyStatusChip, Toast/ToastProvider, StatCard, AgentCard, ClientCard, TicketCard), todos exhibidos en `/design`. Cargada la fuente Geist real (`next/font/google`) y mapeados `statusColors` en Tailwind (pendientes que había dejado 1.1). `diseno-moderno` y `accesibilidad` corridos: se aplicaron todas las correcciones altas — contraste AA de `Button` primary/danger y del tono `primary` de `Badge`/`StatusChip` (nuevo token `primaryTextOnDark` en `brand.config.ts`), `text-faint`→`text-muted` en todo texto con información real, `Select` con flecha visible, `Toast` con pausa al hover/foco, sin auto-cierre en variante `danger`, `role="alert"` para errores, botón cerrar a 44px, animaciones con `motion-safe:`. `typecheck`/`build` limpios. Pendiente menor para fases siguientes: ocultar `/design` antes de producción (ya anotado en 1.1).
- 2026-08-26 · 1.3 · `AppShell`/`Sidebar`/`Topbar` en `app/src/components/layout/` (+ `ConnectionBadge` nuevo en `ui/`), con nav real de docs/eng/05 §3 (operador) y docs/eng/06 §2 (cliente); acento por rol (naranjo/menta) solo en el ítem activo del nav y en `RoleBadge` — la paleta semántica global (violeta=acción, menta=éxito, naranjo=alerta) queda intacta en el resto. Exhibido en `/design/operador` y `/design/cliente`. `diseno-moderno` y `accesibilidad` corridos, correcciones altas aplicadas: bug de borde (`border-accent-warm` pintaba los 4 lados → `border-l-accent-warm`), drawer de navegación mobile nuevo (antes el sidebar desaparecía bajo `md` sin reemplazo — bloqueante para el rol cliente, que no tenía otro modo de navegar en el celular; el drawer cierra con Escape/backdrop/botón y devuelve el foco al trigger), `ConnectionBadge` oculto para el rol cliente (no lo pide docs/eng/06, ese espacio queda para el badge de "sin leer" de tickets), `tabIndex={-1}` en `<main>` para que el skip-link mueva el foco de verdad, avatar con `aria-hidden`, nombre de perfil accesible en mobile (`sr-only`→`sm:not-sr-only`), buscador a 44px sin prometer un atajo `⌘K` inexistente, `role="status"` en `ConnectionBadge`. `typecheck`/`build` limpios, verificado por curl. Fase 1 completa (DoD cumplida). Deuda explícita para fases siguientes: menú de perfil + "cerrar sesión" en el Topbar (espera a Fase 3, auth) y el drawer mobile no tiene trap de foco completo (cicla Tab), solo Escape/backdrop/retorno de foco.
- 2026-08-26 · 2.2 · `db-guardian` hizo una auditoría de cierre sobre el RLS de 2.1 y aplicó `supabase/migrations/20260826141500_rls_ajustes_2_2.sql` (+ rollback) con 4 correcciones: (H1) `audit_log` verdaderamente append-only incluso contra `TRUNCATE` — trigger `app.bloquear_truncate()` + revoke de `TRUNCATE` a `service_role` (las reglas `DO INSTEAD NOTHING` de 2.1 solo cubrían UPDATE/DELETE); (H2) política `tickets_cliente_rw` (que era solo SELECT pese al nombre) renombrada a `tickets_cliente_select` + nuevo RPC `cambiar_estado_mi_ticket` para que el cliente cierre/reabra su propio ticket sin una policy de UPDATE de columnas abiertas; (H3) RPC `actualizar_mi_perfil` para que el usuario edite solo su `nombre` (deliberadamente sin policy de UPDATE self en `usuarios`, para no exponer `rol`/`org_id`/`cliente_id`/`email` a reescritura); (H4) `abierto_por` con default `app.current_usuario_id()` + INSERT que lo exige, para trazabilidad. Verifiqué yo mismo contra la base (tras `db reset` limpio): ambas migraciones aplican en secuencia sin error, `TRUNCATE` bloqueado incluso para el dueño de la tabla, aislamiento cross-tenant confirmado (cliente A no ve/lee/actualiza filas de B), el RPC de tickets deniega sin filtrar existencia, y el `UPDATE` de un cliente contra `audit_log` afecta 0 filas sin lanzar excepción (dato clave para cómo `qa-tester` debe escribir sus asserts en 2.3). Luego `security-auditor` corrió una auditoría formal (48 pruebas adversariales, todas en transacciones con rollback verificado): encontró **2 hallazgos ALTOS**. ALTO-2 (`anon` tenía `EXECUTE` sobre los 2 RPC nuevos porque `revoke all ... from public` no deshace un grant nominal, y Supabase da privilegios por defecto a `anon`/`authenticated` sobre todo objeto nuevo de `public`) lo corrigió él mismo en `20260826163000_hardening_grants_2_2.sql` — revoca esos grants puntuales y además cambia los *default privileges* del schema `public` para que ningún objeto futuro nazca con permisos (falla cerrada en vez de fuga silenciosa). ALTO-1 (`mensajes_ticket` no tenía el flag de "nota interna" que `docs/eng/08` exige — el cliente habría podido leer notas internas del operador en su propio hilo el día que se construya la bandeja de Fase 9) lo corregí yo: `20260826170000_mensajes_nota_interna.sql` agrega `es_interno` + políticas que lo filtran en SELECT y bloquean que el cliente lo marque en INSERT; actualicé `docs/eng/02-modelo-de-datos.md` (tabla, matriz de acceso, ejemplo de política) para que deje de contradecir a `docs/eng/08`. Probado en vivo: operador ve las 3 líneas de un hilo (incluida la nota interna), el cliente ve solo 2, y su intento de insertar marcando `es_interno=true` es rechazado por RLS. Hallazgos MEDIO/BAJO de `security-auditor` quedan como deuda explícita para fases posteriores (no bloquean 2.2): borrado duro sin `deleted_at` en `tickets_reclamos` y `usuarios` (revisar matriz `docs/eng/02` §6.1 con `db-guardian` antes de Fase 9), `/api/run` sin auth/rate-limit/mensajes de error genéricos (antes de Fase 3), `npm audit` con 6 vulnerabilidades altas en `app/` (antes de Fase 11 - `deployment`), falta de índice de idempotencia en `registros_uso` (antes de Fase 7), confirmación de correo de Supabase Auth desactivada (antes de Fase 3), y crear `.gitignore` en la raíz antes de `git init`. DoD de Fase 2 cumplida: RLS probada exhaustivamente, `security-auditor` sin hallazgos altos pendientes.
- 2026-08-26 · 2.3 · `qa-tester` montó Vitest 4 + `pg` (primer test runner del proyecto; `npm run test`/`test:watch` nuevos en `app/package.json`) porque no hay stack de Auth conectado todavía (Fase 3 futura) — el patrón de prueba simula la sesión con `SET LOCAL ROLE authenticated` + `request.jwt.claims` dentro de una transacción Postgres real contra la base local, igual que las verificaciones manuales de 2.2. 52 tests en `app/tests/rls/` (helpers `RlsTx`/`seedTwoOrgs` con fixtures de 2 orgs completas + `global-teardown` que verifica `count(*) = 0` al final y falla la corrida si algo queda pegado) cubriendo: aislamiento cross-tenant en las 8 tablas de negocio (lectura y escritura, cliente y operador, incluyendo el caso positivo de que cada rol sí ve/edita lo suyo — no solo casos de denegación), `mensajes_ticket.es_interno` nunca visible para el cliente ni insertable por él, `audit_log` append-only en sus 3 capas (sin GRANT insert, `DO INSTEAD NOTHING` en update/delete con 0 filas afectadas sin excepción, trigger anti-TRUNCATE incluso para `postgres`), `api_keys_cifradas` sin ningún privilegio directo y `api_keys_publicas` sin exponer material criptográfico, el RPC `cambiar_estado_mi_ticket` sin filtrar existencia de tickets ajenos, y la distinción operador-ve-toda-su-org vs. cliente-acotado-a-su-cliente_id. Verificado independientemente por la sesión coordinadora (no solo el reporte del subagente): corrí `npm run test` (52/52 verde) y `npm run typecheck` (limpio) de nuevo yo mismo, revisé el contenido de los tests (aserciones específicas de rowCount/contenido/errcode, no triviales), y confirmé por `docker exec ... psql` que las 4 tablas clave quedaron en 0 filas tras la corrida. Sin hallazgos de seguridad nuevos — todo coincidió con lo ya corregido en 2.2. **Fase 2 completa: DoD cumplida** (RLS probada exhaustivamente por 3 vías independientes — auditoría de `db-guardian`, auditoría adversarial de `security-auditor`, y ahora suite automatizada de `qa-tester` — y `security-auditor` sin hallazgos altos pendientes).
- 2026-08-26 · 3.1 · Auth con Supabase Auth (server-side, Next.js Route Handlers + `@supabase/ssr`), construido por mí directamente (no hay subagente "builder" para código de app — el flujo del proyecto es que yo implemento y los subagentes revisan/gatean). Archivos nuevos en `app/`: `src/lib/supabase/{server,middleware,cookie-options}.ts`, `src/middleware.ts` (**tiene que vivir en `src/`, no en la raíz de `app/`, porque el proyecto usa carpeta `src/`** — Next.js lo ignora silenciosamente si no; me costó un round de debugging), `src/lib/auth/rate-limit.ts`, `src/app/api/auth/{login,logout}/route.ts`, `src/app/login/page.tsx`, `app/scripts/seed-local-dev.mjs` (cuentas de prueba locales `operador@kaudal.local` / `cliente@kaudal.local`, clave `DevKaudal123!` — solo dev). Verificado end-to-end con curl antes de pedir auditoría: login/logout, gating de `/`, `/design` sigue público, rate limit. Encontré y corregí yo mismo 2 problemas antes de la auditoría: cookie sin `HttpOnly`/`Secure`/`SameSite=Strict` (`@supabase/ssr` no los pone por default) y `/api/run` devolviendo un `307` HTML en vez de `401` JSON al quedar atrapado por el middleware. `security-auditor` hizo una auditoría live con explotación real (no solo lectura de código) y encontró **2 ALTOS**, corregidos y verificados por él: **A1** auto-registro abierto (`enable_signup=true` + el login nunca chequeaba que el `auth.users` tuviera fila en `public.usuarios` — cualquiera en internet podía crear cuenta con la `anon key` pública, loguearse, y quemar `ANTHROPIC_API_KEY` vía `/api/run`; corregido con el chequeo de membresía en `usuarios` en login Y en el middleware, porque un JWT se puede fabricar directo contra Supabase y pegar como cookie, eludiendo el chequeo del login); **A2** el lockout de rate-limit se evadía rotando `X-Forwarded-For` (corregido con un bucket adicional solo-por-correo, que no depende de nada controlable por el atacante). Más 5 **MEDIOS corregidos**: CSRF/fijación de sesión en login (un form cross-site podía loguear a la víctima DENTRO de la cuenta del atacante — corregido con chequeo de `Sec-Fetch-Site`/`Origin` en `src/lib/auth/same-origin.ts`), open redirect vía `?next=` (corregido validando que sea una ruta interna), enumeración de usuarios por timing (220ms cuenta existe vs 85ms no existe, un solo request bastaba — corregido con piso de 450ms en respuestas de credenciales inválidas), el lockout "progresivo" no progresaba (se reseteaba solo al expirar — corregido con memoria de bloqueos por 2h), y cookies de sesión rotadas que se perdían en las ramas de redirect/401 del middleware (bug clásico del patrón `@supabase/ssr`, corregido con un helper que propaga las cookies a cualquier response nuevo). Yo mismo encontré y corregí un bug adicional **en el propio fix de A1**: `security-auditor` había puesto `enable_signup=false` tanto en `[auth]` como en `[auth.email]` de `supabase/config.toml`; verifiqué con curl directo a GoTrue tras el `supabase stop && start` y el login de cuentas EXISTENTES se rompía por completo (`email_provider_disabled`) — ese flag en `[auth.email]` apaga el proveedor de email/password entero en GoTrue, no solo el signup. Lo dejé solo en `[auth]` (que sí basta para bloquear `/auth/v1/signup` sin tocar el login) y reverifiqué ambos comportamientos por separado. También arreglé **M7** (deuda que dejó `security-auditor`, no bloqueante para él pero sí para la DoD): `app/tests/rls/helpers/global-teardown.ts` hacía `count(*)` GLOBAL de las tablas de negocio, así que fallaba siempre que existieran las cuentas persistentes de `seed:local` aunque el `ROLLBACK` de cada test funcionara perfecto; ahora cada conteo queda acotado a filas conectadas al marcador `kaudal-rls-test%`/`@test.kaudal.cl` que ya usan los fixtures de 2.3 (52/52 tests verdes confirmado con las cuentas de seed presentes). Deuda explícita que quedó fuera de 3.1 (documentada, no bloqueante): MFA del operador (M6, antes del primer cliente real), `npm audit` con 6 altas transitivas vía `@mastra/core`/`next` (M8), headers de seguridad `X-Frame-Options`/`nosniff`/`Referrer-Policy` (B4, 6 líneas, antes de Fase 11), rate limit + redacción de errores en `/api/run` (B5), matcher del middleware por prefijo en vez de ancla exacta (B1), whitelist de `/api/auth/*` completa en vez de rutas exactas — hay que revisar antes de agregar endpoints de invitación/reset en Fase 4 (B2), sin expiración absoluta de sesión (B3), password policy subida a 12 caracteres + mayúsculas/minúsculas/dígitos (B6, ya aplicado, relevante de verdad recién en el flujo de invitación de Fase 4). DoD de Fase 3 aún no completa (falta 3.2).
- 2026-08-26 · 3.2 · Roles y rutas protegidas, construido por mí directamente. `src/lib/auth/session.ts` (nuevo) — `obtenerUsuarioActual()` para Server Components/Route Handlers (`{id, orgId, clienteId, rol, nombre, email}`). `src/lib/supabase/middleware.ts` — antes solo sabía "¿tiene acceso a Kaudal?" (booleano); ahora guarda el `rol` y particiona rutas: todo bajo `/portal` es del cliente, todo lo demás (dentro del gate de auth) es del operador; cliente en ruta de operador → redirect a `/portal`, operador en `/portal` → redirect a `/`; rutas de API quedan fuera de la partición por ahora (no hay endpoints de un solo rol todavía); el redirect de `/login` con sesión activa ahora manda al home correcto por rol (antes siempre iba a `/`, rompía la UX del cliente). `src/app/portal/page.tsx` (nuevo, Server Component) — home real del cliente (antes solo existía la muestra en `/design/cliente`), placeholder de bienvenida con `AppShell`, portal completo es Fase 8. `src/app/portal/CerrarSesionBoton.tsx` (nuevo). Verificado end-to-end con curl antes de pedir revisión: operador confinado fuera de `/portal` (307 a `/`), cliente confinado a `/portal` (307 a `/portal` desde cualquier otra ruta, incluido `/login` con sesión activa), sin sesión sigue yendo a `/login?next=...`, `/api/run` sigue funcionando para cualquier rol autenticado. `code-reviewer` revisó los 4 archivos y encontró 1 hallazgo real: `CerrarSesionBoton` no chequeaba `res.ok` ni tenía `try/catch` — si el logout fallaba, el botón igual mandaba a `/login` dejando la cookie de sesión viva (falsa sensación de sesión cerrada en un computador compartido); lo corrigió él mismo con manejo de error vía `useToast` + estado de carga, `typecheck` limpio. 2 observaciones menores sin bloquear (runtime-check opcional con Zod para el tipo `rol`, ya cubierto por el `CHECK` de la migración; y una query duplicada a `usuarios` entre middleware y Server Component en rutas de `/portal`, ya documentada como deuda desde 3.1). **Fase 3 completa: DoD cumplida** (login funciona por ambos lados con destino correcto por rol; `code-reviewer` aprobó con observaciones no bloqueantes).
- 2026-08-26 · 4.1 · Inscripción de clientes, construida por mí directamente. Confirmé primero (`docs/15-mvp-operador.md` + el esquema ya auditado en Fase 2) que el modelo real es **un solo operador, muchos `clientes`** — no una `org` nueva por cliente como sugiere literalmente `docs/eng/00` §3.1/`docs/eng/05` §7 (drift de doc ya conocido, como el de NestJS); "inscribir cliente" crea una fila en `clientes` bajo el `org_id` del operador de sesión, nunca un nuevo `org_id`. Nuevo: `src/lib/chile/rut.ts` (validador módulo 11 — encontré y corregí YO MISMO un bug real antes de pedir revisión: `limpiarRut` no sacaba el guion "-", así que ningún RUT bien formateado como "76.086.428-5" pasaba el regex; verificado con curl antes/después), `src/lib/supabase/admin.ts` (cliente `service_role`, usado ÚNICA Y EXCLUSIVAMENTE para `auth.admin.inviteUserByEmail`/`deleteUser` — toda lectura/escritura de `clientes`/`usuarios` pasa por el cliente de sesión, así que la autorización real la sigue dando RLS, no un chequeo manual), `src/app/api/clientes/route.ts` (POST: valida con Zod+RUT, inserta `clientes`, invita al contacto, inserta `usuarios` rol=cliente, con compensación —borra lo creado— si cualquier paso de en medio falla, ya que no hay transacción distribuida entre Postgres y la Admin API de Auth), `src/app/clientes/page.tsx` (lista) y `src/app/clientes/nuevo/page.tsx` (formulario) usando `AppShell` de verdad por primera vez para una función real (no una muestra de `/design`). También encontré y corregí un segundo bug propio: el link de invitación de Supabase llegaba apuntando a `/` en vez de `/invitacion` porque mi `redirectTo` no calzaba con `additional_redirect_urls` de `supabase/config.toml` (GoTrue lo descarta en silencio y vuelve a `site_url`) — corregido con un wildcard `http://.../**`, verificado leyendo el correo real en Mailpit antes y después del fix. `diseno-moderno` revisó las pantallas: corrigió la columna "Estado" que no tenía color (nuevo componente `ClientStatusChip`, mismo patrón que `KeyStatusChip`), fusionó nombre+RUT en una sola celda de tabla (así lo define `docs/eng/05` §6 y así lo hace `ClientCard`), y creó `InfoNote` (nuevo componente reutilizable, tono `info`) para que la nota de seguridad de la API key tuviera la jerarquía visual que le corresponde en vez de una caja neutra cualquiera. `code-reviewer` revisó el backend: confirmó que el límite `service_role`/RLS está bien puesto, y corrigió un gap real — la compensación (`deshacerCliente`/`deleteUser`) ignoraba sus propios errores, así que si el rollback mismo fallaba quedaba un `clientes` huérfano e invisible (un RUT repetido después daría un 409 confuso sin pista de por qué); ahora deja rastro con `console.error` (no hay logging estructurado todavía). Verificado por mí de punta a punta después de ambas revisiones: `typecheck` limpio, 52 tests de RLS en verde, y un tercer ciclo completo de inscripción por curl (crear cliente → fila en `clientes`+`usuarios` → invitación real en Mailpit con el link correcto), con limpieza de los 3 clientes de prueba al terminar (queda solo el `Empresa Demo SpA` del seed). Deuda documentada, no bloqueante para 4.1 pero sugerida por `code-reviewer` antes de que esto entre en producción: pasada de `security-auditor` sobre el flujo de invitación/`service_role` (hoy solo lo revisó `code-reviewer`, no una auditoría de seguridad dedicada), casos de `qa-tester` para los dos caminos de compensación (falla el invite / falla el insert de usuarios), tipar los clientes de Supabase con `<Database>` (gap preexistente de todo el proyecto, no de esta tarea), y decisiones de alcance deliberadamente pospuestas: el toggle "enviar invitación ahora" de `docs/eng/05` §7 no existe (siempre se invita al crear) y "Plan" quedó como texto libre en vez de `Select` (no hay catálogo real de planes hasta Fase 10/Flow). Falta **4.2** (`/invitacion` — la página que completa el flujo, el link ya apunta ahí pero la ruta no existe todavía) para poder decir "el cliente puede entrar" (DoD de Fase 4).
- 2026-08-26/27 · 4.2 · Primera versión (construida por mí) usaba `createBrowserClient` para procesar el link de invitación del lado del navegador (patrón estándar de Supabase, pero **roto en la práctica**: verifiqué "end-to-end" con curl llamando directo a los endpoints de GoTrue, lo cual probaba que Supabase funcionaba pero NUNCA ejercitaba el código real de la app). `security-auditor` encontró el fallo de verdad (ALTO, bloqueante): `createBrowserClient` fuerza `flowType: "pkce"` de forma no sobreescribible, y el link de `inviteUserByEmail` es de flujo implícito (`#access_token=...`) — `auth-js` **rechaza explícitamente** un callback implícito en un cliente PKCE ("Not a valid PKCE flow url."), así que `getSession()` siempre devolvía `session: null` sin error visible y todo cliente invitado se habría encontrado con "Este link no es válido" para siempre. Lo reprodujo instrumentando el paquete real en Node. Además encontró que mi propia mitigación documentada para la cookie-sin-HttpOnly tampoco era cierta: no se "sana" en el siguiente request como yo creía — `@supabase/ssr` solo reescribe la cookie en eventos `SIGNED_IN`/`TOKEN_REFRESHED`/etc., así que con un access token recién emitido la cookie legible por JS sobrevivía hasta el `jwt_expiry` completo (1 hora), no "un instante". **Reescribí el flujo completo** (arquitectura que propuso `security-auditor`, server-first de punta a punta): plantilla de correo propia (`supabase/templates/invite.html`, `config.toml` `[auth.email.template.invite]`) que arma un link a `/auth/confirmar?token_hash=...&type=invite` en vez del link default de Supabase (que apunta a `/auth/v1/verify` de GoTrue, formato implícito); `src/app/auth/confirmar/route.ts` (nuevo, Route Handler `GET`) canjea el `token_hash` con `verifyOtp()` sobre `crearClienteServidor()` — la cookie de sesión queda `HttpOnly` real desde el primer byte, nunca pasa por un cliente de navegador ni por el fragmento de la URL; `/invitacion` pasó a ser Server Component (solo decide qué mostrar según si `getUser()` devuelve sesión) + `FormularioClave.tsx` (client, solo hace `fetch` a `src/app/api/auth/fijar-clave/route.ts`, nuevo Route Handler con el mismo patrón de `esMismoOrigen`/mensajes de error traducidos que login/logout); **borré** `src/lib/supabase/client.ts` (el cliente de navegador, sin usos tras el rediseño — eliminar en vez de dejarlo como trampa para el próximo que necesite "algo del lado del cliente"). Middleware: agregué `/auth/confirmar` a rutas públicas y exceptué `/invitacion` del particionado por rol de 3.2 (si no, un cliente recién confirmado rebotaba a `/portal` antes de poder fijar su clave — edge case que el propio `security-auditor` había anotado). Otros hallazgos de la misma auditoría, ya corregidos: mensajes de error de GoTrue mostrados crudos en inglés al usuario (traducidos por `error.code`, nunca `error.message`); validación de complejidad de contraseña solo del lado servidor (agregado un espejo client-side de `minimum_password_length=12`+`lower_upper_letters_digits` para no gastar un viaje al servidor con una clave obviamente débil); rutas públicas por prefijo en vez de exactas (endurecido a `RUTAS_PUBLICAS_EXACTAS` vs `PREFIJOS_PUBLICOS`, para que un futuro `/invitacion/algo` no naciera público sin que nadie lo decidiera). Verificado por mí, esta vez **de verdad** de punta a punta contra el servidor real (no contra GoTrue directo): operador inscribe cliente → correo con el link nuevo llega a Mailpit → `GET /auth/confirmar` con cookie jar → `Set-Cookie` con `HttpOnly` confirmado en el header → `GET /invitacion` muestra el formulario → `POST /api/auth/fijar-clave` → `GET /portal` da 200 → login normal con la clave nueva funciona. `typecheck` limpio, 52 tests en verde, datos de prueba limpiados. Deuda documentada, no bloqueante: `[auth.sessions] timebox`/`inactivity_timeout` sin definir (sesión sin caducidad) y `secure_password_change=false` — decidir antes de producción; un operador con sesión en el mismo navegador puede abrir `/invitacion` de otro (el confinamiento por rol de 3.2 solo aplica a `cliente`) — sin impacto práctico hoy, revisar si se construye una pantalla de "cambiar mi contraseña" más adelante. **Fase 4 completa: DoD cumplida** (el operador inscribe un cliente real y ese cliente entra con su propia contraseña, verificado genuinamente de punta a punta, no solo contra la API de Auth).
- 2026-08-26/27 · 5.1 · Cifrado de API keys del cliente (AES-256-GCM). **`db-guardian`** construyó primero el esquema/RPC (`supabase/migrations/20260826220500_api_keys_rpc_5_1.sql`): columna `api_keys_cifradas.key_version` (versión de la clave maestra, para rotarla sin re-cifrar todo — docs/eng/03 §2.4) y dos RPC `SECURITY DEFINER` — `guardar_api_key_cliente` (alta/rotación: si ya hay una key activa del mismo proveedor+alias la marca `revocada` y crea la nueva con `rotada_de` apuntando a la vieja) y `revocar_api_key_cliente` — ambas exigen `app.current_rol()='cliente'` y toman `org_id`/`cliente_id` de la sesión, nunca de un parámetro; la tabla base sigue con CERO privilegios directos para `authenticated`. Encontró y corrigió él mismo un hallazgo ALTO en sus propias pruebas: un CHECK violation en el INSERT filtraba la fila completa —ciphertext, iv, auth_tag incluidos— en el `DETAIL` del error de Postgres, que PostgREST reenvía al cliente. Ambas RPC quedan auditadas en `audit_log` (solo metadatos). **Yo** construí el resto: `src/lib/crypto/{master-key,api-keys}.ts` (AES-256-GCM nativo de Node, IV de 12 bytes por `randomBytes`, clave maestra versionada en env `KAUDAL_MASTER_KEY_V1`/`KAUDAL_KEY_VERSION_ACTUAL`, nunca en el repo), `src/lib/proveedores/validar-api-key.ts` (formato por proveedor + ping real de prueba contra Anthropic/OpenAI antes de guardar), `src/app/api/portal/api-keys/route.ts` (POST, solo rol cliente, rate-limit 10/min por org, bytea codificado como `'\x'+hex` como pidió `db-guardian`), y las pantallas del portal (asistente de 3 pasos si no hay key activa, `/portal/cuenta` con estado + reemplazar). Verifiqué el cifrado de forma genuina (no solo "se ve cifrado"): extraje ciphertext/iv/tag de una fila real y los descifré con un script Node aparte usando la misma clave maestra, recuperando el texto plano exacto. **`security-auditor`** (bloqueante) hizo pruebas reales por HTTP/DB (200.000 IVs de `randomBytes(12)` sin colisión, fidelidad byte-exacta de bytes "peligrosos" `0x00`/backslash/comilla/`0xff` a través de PostgREST, manipulación de ciphertext/iv/tag/clave-equivocada todas rechazadas, y una ruta temporal propia que lanzaba una excepción con un secreto falso para confirmar que Next.js en dev NO devuelve stack traces con datos sensibles — confirmado, 500 con body vacío). Encontró y corrigió 5 MEDIOS/BAJOS: **CSRF ausente** en el endpoint (regresión respecto al patrón ya usado en 3.1/4.2 — agregado `esMismoOrigen`); **`.maybeSingle()` sin `.limit(1)`** en `/portal` y `/portal/cuenta` — como el RPC solo revoca la key anterior del MISMO proveedor+alias, conectar Anthropic y luego "reemplazar" eligiendo OpenAI dejaba 2 keys activas simultáneas, `maybeSingle()` recibía 2 filas (error PGRST116), y el cliente quedaba en loop viendo el onboarding de nuevo pese a tener una key conectada — corregido con `.order(created_at desc).limit(1)`; **`probarApiKey` sin timeout/sin bloquear redirects** — un 30x del proveedor podía reenviar el header `x-api-key` (a diferencia de `Authorization`, que sí se descarta cross-origin por spec) a otro host — corregido con `redirect:"error"`, `cache:"no-store"`, timeout de 8s; `.strict()` en el Zod del body; validación de entero en la versión de clave maestra. Dejó 2 hallazgos **obligatorios antes de producción, no bloqueantes para el merge** (anotados arriba en la casilla de la tarea): rate-limit real dentro del RPC (hoy el endpoint de PostgREST es alcanzable directo saltándose la app) y cabeceras de seguridad/CSP. **`compliance-cl`** (bloqueante, Ley 19.628/21.719 — vigencia 1-dic-2026) dio **aprobado con observaciones**: distinguió que la API key en sí es secreto de negocio, no dato personal (así que sus hallazgos de retención son deuda contractual/de producto, no incumplimiento legal directo), pero marcó 2 ALTOS de copy/UX que sí corregí en el momento: (1) el formulario decía "ni Kaudal ni tu operador la ven en texto" sin aclarar que la key SÍ viaja en claro al proveedor (Anthropic/OpenAI, fuera de Chile) para validarla — copy reescrito explicando el envío al proveedor y que "revocar en Kaudal" no revoca nada allá; (2) la RPC `revocar_api_key_cliente` existía desde la propia migración de 5.1 pero nada la llamaba — el cliente solo podía "reemplazar", nunca desconectar sin poner otra key — agregué `DELETE /api/portal/api-keys/[id]` + botón "Desconectar" con confirmación en Mi cuenta. Quedan como deuda documentada (correctamente diferida, según el propio `compliance-cl`): anonimización de `audit_log` para soportar supresión del titular (bloqueante ANTES de construir la baja de clientes, no ahora — las reglas `DO INSTEAD NOTHING` hacen que hoy un `UPDATE`/`DELETE` de anonimización falle en silencio, 0 filas sin error), *crypto-shredding* de keys revocadas tras N días (requiere relajar los `CHECK` de longitud no-vacía), `key_fingerprint` sin caso de uso real todavía, el cliente no puede ver su propio historial de auditoría, y falta página legal/DPA con abogado. Verificado por mí al final, con todos los cambios ya aplicados: `typecheck` limpio, 52 tests en verde, ciclo completo por curl (conectar → aparece en `/portal` y `/clientes` del operador → CSRF cross-site bloqueado 403 → rotar → desconectar → `estado=revocada` en la BD), aislamiento entre dos clientes reales confirmado. Nota operativa: `audit_log` es append-only de verdad (ni `postgres` puede borrar sus propias filas de prueba) — cuando una prueba genera entradas de auditoría, la única forma limpia de "resetear" en dev es `supabase db reset` + `npm run seed:local`, no un DELETE a mano (se probó y falla por FK). **Sin hallazgos ALTOS de ninguna de las dos revisiones bloqueantes.**
- 2026-08-27 · 5.2 · En gran parte ya cubierto por lo construido en 5.1 (el portal del cliente ya mostraba `••••last4` en `/portal/cuenta` y el operador ya tenía el chip de estado en `/clientes`). Lo que faltaba puntualmente, per `docs/18` §9 ("el operador ve solo la key **enmascarada** (ej: sk-...ab12)"): la lista `/clientes` solo mostraba el chip `Key OK`/`Sin key`, no la key enmascarada en sí. Agregado: `keyPorCliente` (Map desde `api_keys_publicas`, que ya trae `proveedor`/`key_last4` sin ciphertext) y una línea bajo el chip con `{Proveedor} ••••{last4}` — verificado con curl que se renderiza (el grep sobre el HTML crudo fallaba varias veces por los comentarios de límite de hidratación de React entre el texto y el `••••`, no por un bug real; confirmado leyendo el HTML completo). Estado `inválida` del `KeyStatusChip` ya existe en el vocabulario del componente (tono `danger`) pero nada lo dispara todavía — no hay verificación periódica ni un evento que invalide una key sola; eso depende de Fase 7 (uso y costo, cuando un agente reporte que la key le falló) o de construir `POST /api/keys/:id/test` (`docs/eng/03` §3, no implementado). No lo considero bloqueante para 5.2: el estado existe y se puede activar cuando haya una señal real que lo dispare, mostrar "inválida" sin una señal real sería peor (falso negativo). `typecheck` limpio, 52 tests en verde, datos de prueba limpiados. **Fase 5 completa: DoD cumplida** — sin hallazgos altos de `security-auditor` en ninguna de las dos tareas, la key nunca aparece en claro en red (solo cifrada en tránsito hacia el proveedor para validarla, nunca hacia el frontend) ni en logs (verificado con una excepción no capturada real: 500 con body vacío, sin stack).
- 2026-08-27 · 5.1 (seguimiento) · Cerrado el único pendiente "obligatorio antes de producción" que el usuario pidió resolver ahora: el RPC `guardar_api_key_cliente`/`revocar_api_key_cliente` era alcanzable directo por PostgREST saltándose el rate-limit de la app (10/min por org, en memoria del proceso Next.js — un POST directo a `/rest/v1/rpc/...` nunca pasa por ese código). `db-guardian` agregó `app.exigir_cupo_api_keys(org, cliente)` en `supabase/migrations/20260827093000_limite_api_keys_rpc.sql`: 30 acciones `api_key.*` por hora **por `cliente_id`** (no por org, para no castigar a los demás clientes de la misma empresa; no por `actor_id` suelto, para que no se evada creando usuarios nuevos del mismo cliente), contadas sobre el propio `audit_log` que ya escriben ambas RPC — decisión deliberada de no crear una tabla de rate-limit nueva ("sería un segundo registro del mismo hecho, que además habría que purgar, y purgar es justo lo que `audit_log` no permite"). Dos detalles no triviales que agregó por su cuenta: un **advisory lock transaccional por cliente** antes de contar (sin él, N llamadas en paralelo leen el contador antes de que cualquiera confirme y todas pasan — verificado con dos sesiones concurrentes reales); y el error usa `errcode='PT429'`, que PostgREST traduce nativamente a HTTP 429 (verificado por HTTP real con una función-sonda temporal, creada y borrada sin dejar rastro). Solo cuenta acciones que **sí** se completan (una llamada rechazada no escribe auditoría, así que no consume cupo ni hace crecer nada). Yo agregué el mapeo que faltaba del lado de la app (`error.code === "PT429"` → 429 con el mensaje real, en vez de cascabelear a un 500 genérico) en ambas rutas (`api/portal/api-keys/route.ts` y `.../[id]/route.ts`) y lo verifiqué de punta a punta con HTTP real (no solo transacciones de prueba): creé un cliente descartable, quemé 30 altas reales vía SQL directo simulando su sesión, y la llamada 31 **a través del endpoint HTTP real de la app** devolvió `429 {"error":"Demasiados intentos..."}` en vez de un 500 — confirma que el mapeo nuevo funciona con el camino de ejecución real, no solo en teoría. Confirmé que un segundo cliente no se ve afectado por el bloqueo del primero. `npm run typecheck` limpio, 52 tests en verde. Como esta prueba necesitaba dejar 30+ acciones reales y permanentes en el `audit_log` (append-only) de un cliente descartable, terminé con `supabase db reset` + `npm run seed:local` para volver a una base limpia en vez de intentar un cleanup parcial. Queda pendiente, y sigue siendo la única cosa "obligatoria antes de producción" de toda la Fase 5: cabeceras de seguridad/CSP en `next.config.mjs`.
- 2026-08-27 · 6.1/6.2 · Registrar un agente que ya corre (endpoint/webhook + healthcheck + modelo/canal/estado). Confirmé primero, como en 4.1, que el spec (`docs/eng/01` §4, `docs/eng/05` §9-10) asume NestJS/`org_id`-por-cliente (drift ya conocido) y adapté al modelo real ya construido. `db-guardian` extendió el esquema de `agentes` (`supabase/migrations/20260827160000_agentes_registro_healthcheck.sql`): columnas `canal` (whatsapp/web/api/otro), `health_url`/`ultimo_healthcheck_en`/`ultimo_healthcheck_ok`/`healthcheck_fallos_consecutivos` (este último no lo pedí yo — lo agregó porque `docs/eng/01` §4.2 exige "si falla N veces seguidas → caído" y no había dónde persistir el conteo), `estado` ganó el valor `caido` (antes solo activo/pausado/archivado), y el secreto de auth del endpoint del agente cifrado con el mismo patrón que `api_keys_cifradas` (`auth_tipo`/`auth_ciphertext`/`auth_iv`/`auth_tag`/`auth_version`/`auth_header_nombre`, con CHECKs de consistencia — ej. `header_key` sin nombre de header rechazado, `bearer` sin ciphertext rechazado). A diferencia de Fase 5 (que usa RPC `SECURITY DEFINER` para escribir), acá `db-guardian` decidió **GRANT por columna** sobre la tabla base (`revoke select` + `grant select` de una lista blanca que excluye las 4 columnas de auth cifrada y `ingest_token_hash`, que también sacó del GRANT preexistente desde 2.1 — nadie lo necesitaba en el frontend) más la vista `agentes_publicos` como alternativa de lectura — las escrituras quedan sin restringir por columna porque el operador cifra en el Route Handler y escribe bytes opacos en su propia org, sin necesidad de una RPC intermedia. Yo construí el resto: `src/lib/agentes/probar-conexion.ts` (ping server-side), `src/lib/crypto/ingest-token.ts` (token que el agente usa para reportar uso — `randomBytes(24)` + hash SHA-256, se muestra UNA vez), `src/app/api/agentes/route.ts` + `.../probar-conexion/route.ts` + `src/app/api/clientes/[id]/api-keys/route.ts`, y el wizard de 3 pasos (`src/app/agentes/nuevo/`) + lista (`src/app/agentes/page.tsx`) + `AgenteEstadoChip` (nuevo, vocabulario real activo/pausado/caído/archivado, distinto del `AgentStatusChip` genérico de Fase 1). Verifiqué el cifrado del secreto del agente de forma genuina (extraje ciphertext/iv/tag reales con `docker exec psql`, tamaños GCM correctos) y que el hash del ingest token coincide con `sha256(token)` calculado aparte. `diseno-moderno` corrigió 3 cosas: el indicador de pasos del wizard (era texto plano, ahora `<ol>` con círculos numerados + `aria-current`), los estados de "Probar conexión" (texto plano → `StatusChip` con tono/pulse, siguiendo el patrón ya usado en `ConnectionBadge`), y el peso visual del bloque del ingest token (tono `warning` + ícono, coherente con cómo `ConectarApiKeyForm` trata sus notas de seguridad). `code-reviewer` aprobó y extrajo `aBytea()` (estaba duplicada) a `src/lib/crypto/bytea.ts` compartido. **`security-auditor` encontró un ALTO real: SSRF** — el "Probar conexión" hace un `fetch` desde el SERVIDOR de Kaudal hacia una URL que el operador escribe libremente; `https://` en Zod no protegía nada (un dominio propio apuntando a `127.0.0.1` es https válido), así que un operador (o una sesión de operador robada, combinado con el CSRF que faltaba) podía usar el endpoint como oráculo hacia el metadata endpoint de la nube (`169.254.169.254` → credenciales IAM → acceso a la infraestructura de TODOS los tenants) o para escanear la red interna del servidor. Corregido con `urlDeAgentePermitida()`: resuelve el hostname por DNS ANTES del fetch y exige que todas las IPs resueltas sean unicast público (lista negra de rangos RFC1918/loopback/link-local/CGNAT/etc. para IPv4, lista blanca `2000::/3` para IPv6, excluyendo túneles 6to4/Teredo que encapsulan una IPv4). Verificado por mí con curl real: `169.254.169.254` rechazado, la IP ofuscada como entero `2130706433` (=127.0.0.1, un bypass clásico) también rechazada, `1.1.1.1` pasa la validación. Se valida tanto `endpointUrl` como `healthUrl` al crear (no solo la que se usa para el ping), confirmado con curl: un `healthUrl` legítimo con un `endpointUrl` malicioso queda igual rechazado. Además corrigió: CSRF ausente en ambas rutas nuevas (`esMismoOrigen`, mismo patrón de Fase 5); sin rate-limit (`docs/eng/03` §7 pide 10/min en `/test` — agregado); un CHECK de header-name que permitía cabeceras de control del transporte (`Host`/`Cookie`/`Content-Length`) sobrescribirlas; el VALOR del secreto de auth sin validar contra el field-value de HTTP (un `\r\n` no era inyección pero dejaba un secreto inutilizable cifrado y guardado para siempre); y **un bug real de escape en `bytea.ts`** (`` `\x${...}` `` es una secuencia inválida — `npm run typecheck` daba falso verde por la caché incremental de `tsconfig.tsbuildinfo`, con la caché borrada fallaba). Verificado por mí con caché borrada: `typecheck` limpio de verdad. Sin hallazgos altos abiertos — **aprobado**. Dos seguimientos no bloqueantes, agregados como ítems reales al tablero: **6.3** (`audit_log` sin una sola escritura en toda la app, hueco transversal desde Fase 2, no específico de esta fase) y una nota en **11.2** (CI debe correr `typecheck` sin caché incremental). `npm run typecheck` (con caché fresca) y `npm run test` (52/52) en verde, datos de prueba limpiados. **Fase 6 completa: DoD cumplida** (agente registrado, estado real reflejado — `activo` cuando el ping pasa, `caido` cuando no).
- 2026-08-27 · 7.1 · Endpoint `POST /api/usage/events`, el primero del proyecto que NO usa sesión de Supabase Auth (lo llama un agente externo con su propio `ingest_token` Bearer) — un webhook público de verdad. `db-guardian` extendió `registros_uso` (ya existía desde Fase 2, docs/eng/07 la llama `usage_events` pero es la misma tabla) con `idempotency_key` (único parcial por `(agente_id, idempotency_key)`, con CHECK de formato porque el valor llega por header) y `status`; creó `model_pricing` (nueva, catálogo GLOBAL sin `org_id` — única excepción a la regla de `CLAUDE.md` hasta ahora, justificada porque son precios de lista públicos de los proveedores, no datos de ningún tenant), sembrada con los 5 modelos de `tools/calculadora-agentes.html` (eligió los identificadores reales de API con cuidado de que cada uno calzara con su precio real, ej. `claude-opus-4-1` para la tarifa de Opus, no un genérico "claude-opus"). Construí el endpoint: resuelve el agente por hash del token con el cliente ADMIN (única vía posible, no hay `auth.uid()`), calcula costo real contra `model_pricing` cuando hay tokens reportados, deja costo en 0 (no inventa un número) cuando no hay perfil de tokens promedio del agente, e idempotencia real (reintento con la misma `Idempotency-Key` devuelve el registro existente en vez de duplicar). **Encontré y corregí yo mismo un bug real antes de pedir revisión**: el middleware global exige sesión de cookie para todo `/api/*` no listado explícitamente, y este endpoint no estaba — cualquier request (con o sin token válido) recibía el 401 genérico del middleware antes de que la ruta llegara a mirar el `Authorization`. `code-reviewer` encontró y corrigió 2 bugs reales: la respuesta mapeaba `source` con el valor en español de la BD (`"reportado"`) en vez del contrato documentado (`"reported"` — cualquier integrador codeando contra el ejemplo literal de `docs/eng/07` habría fallado en silencio); y el `model` del evento no se normalizaba a minúsculas antes de cruzar contra `model_pricing` (que sí las exige por CHECK), así que `"Claude-Sonnet-4-5"` calculaba costo 0 sin ningún error visible. `security-auditor` encontró **1 ALTO**: mi fix del bug del middleware lo hice por PREFIJO (`/api/usage`), dejando pública TODA la familia futura (`/usage/summary`, `/by-agent`, `/by-day`, `/where` de `docs/eng/07` §2.4 — lecturas de datos de clientes) sin que nadie lo decidiera; lo acotó a la ruta exacta `/api/usage/events`. Más 5 MEDIOS: sin cotas superiores en tokens/unidades (aceptaba `3e9`, envenenando `costo_estimado`); `occurred_at` sin ventana temporal (un token filtrado podía fechar eventos en 2099 o en un período ya facturado); sin límite de tamaño de body (los Route Handlers del App Router NO aplican límite automático, a diferencia de las API routes viejas — un token válido + 120 req/min × N MB es almacenamiento gratis); un agente `pausado`/`archivado` seguía aceptando uso con su token (no había NINGUNA vía de revocación salvo borrar el agente); y el `model` sin acotar por formato (además de la normalización de `code-reviewer`, agregó un regex que bloquea caracteres de control/inyección de fórmula CSV, dado que este texto se persiste y se va a exportar). También encontró que el rate-limit por `agente_id` solo corre DESPUÉS de resolver el token — cualquiera probando tokens al azar no pagaba ningún costo por intento fallido (no es fuerza bruta viable con 192 bits de entropía, pero sí amplificación/DoS contra Postgres); agregó un segundo rate-limit por IP ANTES de la resolución del token, y en el proceso encontró que el `Map` de rate-limit genérico no tenía poda y ahora recibía claves derivadas de IP (falsificable vía XFF) — le agregó tope y poda para que no fuera un vector de agotar memoria. `qa-tester` dejó 24 tests nuevos (`app/tests/usage-events/`, primeros del proyecto que NO dependen de simular sesión — usan HTTP real contra el servidor de dev a propósito, para ejercitar también el middleware, que es justo la capa que falló dos veces sin que nadie lo detectara) cubriendo cada uno de los casos de arriba, incluyendo el costo exacto calculado, el bug de `source` como regresión explícita, y las dos capas de rate-limit. Sin ningún comportamiento inesperado encontrado. Verificado por mí al final: `typecheck` limpio (con caché borrada), 76/76 tests en verde (52 de RLS + 24 nuevos), y una ronda propia de curl contra el servidor real repitiendo los casos clave. Base limpia en todo momento (el `global-teardown` de `qa-tester` filtra por marcador, nunca toca datos reales). Deuda documentada para antes de producción, anotada en la casilla de la tarea: proxy de borde para `X-Forwarded-For` real, Redis si hay más de una instancia, decidir si la idempotencia debe ser obligatoria, y rotación de `ingest_token`.
- 2026-08-27 · 7.2 · Porté `tools/calculadora-agentes.html` (simulador standalone con precios de modelos hardcodeados en un array JS) a `/calculadora` (operador, nuevo ítem de nav + ícono `CalculatorIcon`): `src/app/calculadora/page.tsx` trae tarifas reales de `model_pricing` (Fase 7.1) y `CalculadoraAgentes.tsx` reimplementa la misma fórmula en React — sigue siendo un simulador puro (nada se persiste, no llama a ningún endpoint; decidí NO construir el `POST /usage/estimate` de `docs/eng/07` §3.4 porque nada lo consume todavía). `diseno-moderno` corrigió 3 cosas reales: el tono "ajustado" del veredicto usaba `accent-warm` (naranjo, reservado en todo el sistema para diferenciar el rol operador) en vez de `warning` (ámbar, el que corresponde); los emojis (⚙️💸🧾📈✅⚠️⛔) no siguen el patrón del resto de la app (que usa el set de íconos de línea de `layout/icons.tsx`, nunca emoji como iconografía recurrente) — los reemplazó por 3 SVG inline del mismo lenguaje visual; y el slider de "múltiplo de margen" (primer `<input type="range">` del código base) no tenía foco visible ni `label` asociado — agregado. `code-reviewer` aprobó sin bugs de correctitud (verificó la conversión de unidades USD/1k↔USD/1M de punta a punta, el patrón de inicializador perezoso de `useState`, y que `precioInput` como string distingue bien "vacío" de `"0"` explícito) y agregó un `min={0}` faltante. Ambos señalaron deuda no bloqueante: el cliente de Supabase sigue sin tipar con `<Database>` (gap preexistente y transversal a los 14 archivos que usan `.from(...)`, no de esta tarea), el spec dice que la calculadora es para operador Y cliente pero quedó operador-solo (decisión de alcance MVP, anotarlo en el doc), y sigue sin haber ni un test en todo `app/src` (los 76 tests existentes son todos de API/RLS, ninguno de una pantalla — se sugiere extraer la fórmula a una función pura testeable si se quiere cubrir después). `typecheck` limpio (caché borrada), 76 tests en verde, verificado con curl que la pantalla renderiza con los cambios de `diseno-moderno` aplicados.
- 2026-08-26 · 2.1 · Supabase LOCAL vía Docker (`npx supabase start`, puertos corridos a 55321-55329 porque otro proyecto en la máquina ya usaba los por defecto; keys en `app/.env.local`, gitignored). `db-guardian` escribió y aplicó `supabase/migrations/20260826125600_esquema_inicial.sql` (646 líneas: 11 tablas, 19 políticas RLS, 3 funciones helper `SECURITY DEFINER`, 10 triggers `updated_at`, vistas `uso_diario`/`api_keys_publicas`) + rollback en `supabase/rollbacks/` (probado: down completo, vuelta a aplicar sin errores). Verificado independientemente (no solo el reporte del subagente): 13 relaciones en `public` (11 tablas + 2 vistas), RLS `ENABLE` en las 11 y `FORCE` en 10 (`usuarios` sin `force` — necesario para que los helpers `SECURITY DEFINER` no se autobloqueen), 19 políticas, FKs compuestas en `agentes` confirmadas, `authenticated` con cero privilegios sobre `api_keys_cifradas`. **`db-guardian` encontró y corrigió 5 problemas críticos del DDL de `docs/eng/02` que tal cual escrito habrían roto el aislamiento multi-tenant o bloqueado la app entera**: (1) la vista `uso_diario` sin `security_invoker` filtraba uso de TODAS las orgs; (2) `api_keys_publicas` + la política `using(false)` de §6.3 se contradicen (la vista no devolvía nada) — resuelto con vista `SECURITY DEFINER` que filtra tenant internamente + revoke total sobre la tabla base; (3) los helpers de sesión (`app.current_org_id()` etc.) no eran `SECURITY DEFINER`, con RLS en `usuarios` habrían devuelto NULL siempre y tumbado toda la app; (4) faltaba integridad en el `org_id` denormalizado — sin FKs compuestas, un bug de backend podía insertar un agente con `org_id` de una org y `cliente_id` de otra y la RLS se lo mostraría a la org equivocada (el fix más importante); (5) los grants por defecto de Postgres dan `TRUNCATE` a `anon`/`authenticated` en tablas nuevas, y `TRUNCATE` ignora RLS — revocado todo. También completó índices/checks/`ON DELETE` que el DDL de §8 omitía pero el resto del doc sí pedía. Detalle completo del reporte queda en la transcripción del subagente; **pendiente: pasar estas correcciones al propio `docs/eng/02-modelo-de-datos.md` con `docs-writer`** para que el doc deje de estar desincronizado del esquema real. Falta `supabase/seed.sql` (antes de 2.2/2.3, para tener fixtures estables). Pruebas de humo (rollback tx) confirmaron aislamiento básico como adelanto de la 2.3.
- 2026-08-27 · 7.3 · Pantalla del cliente "Dónde se usa" (`/portal/uso`, Server Component). `db-guardian` agregó `clientes.limite_mensual_clp numeric(14,2)` nullable (CHECK `>=0`, más un CHECK contra overflow en `>=1e12` con mensaje en español que no pedí pero tiene sentido — un input mal tipeado con un dígito de más no debe reventar con un error de Postgres crudo) y el RPC `actualizar_limite_mensual_cliente(p_monto numeric default null)` (mismo patrón `SECURITY DEFINER` cliente-only que `actualizar_mi_perfil`; el `default null` deja que un POST sin body también sirva para "borrar mi límite", sin un endpoint aparte). Construí `page.tsx` (trae `clientes.limite_mensual_clp` + `uso_diario` del mes + `agentes` para nombre/modelo, agrega todo en JS — no hay vista nueva, `uso_diario` ya viene filtrada por RLS desde Fase 2), `UsoPorDia.tsx` (barras CSS puras, `role="img"`+`aria-label` con una `<table class="sr-only">` como alternativa accesible — mismo espíritu que el Sparkline de `AgentCard`), `LimiteMensual.tsx` + `POST /api/portal/limite-mensual` (mismo patrón `esMismoOrigen`+Zod+RPC que el resto del portal). **Encontré y corregí yo mismo un bug real antes de pedir revisión**: `db-guardian` había advertido explícitamente en su reporte que `0` y `null` son estados distintos para `limite_mensual_clp` (0 = "avísame apenas gaste algo", null = "sin límite configurado, nunca avises") — mi primer borrador comparaba con `?.limite_mensual_clp ?` (truthiness de JS, trata `0` como si fuera "sin límite"); corregido a `!= null` explícito y verificado con curl contra la base real para los 4 casos (límite 0, límite bajo ya superado, límite alto sin superar, límite borrado/null). `diseno-moderno` mejoró: el aviso de límite pasó de texto plano a un bloque con ícono de alerta + `p-4`/`flex items-start gap-3`, tono `warning`/`danger` diferenciado según el porcentaje; el gráfico de barras cambió de `bg-primary-hover` (reservado para estados `:hover` en el resto del sistema) a `bg-primary`, subió la altura mínima visible de barra 4%→6%, y agregó la línea "Día con más uso" siempre visible (el `title` nativo no es alcanzable por touch ni teclado); el botón de guardar límite pasó de `variant="secondary"` a `primary` (es la única acción del formulario, mismo criterio que `ConectarApiKeyForm`). `accesibilidad` corrigió: los errores de validación del formulario de límite ahora usan el prop `error` de `Input` (persistente, con `aria-invalid`/`aria-describedby`) en vez de solo un toast transitorio que desaparece; el aviso de límite ganó severidad real en el anuncio a lectores de pantalla (`role="alert"`/`aria-live="assertive"` al llegar a 100%, `role="status"`/`aria-live="polite"` entre 80-99%, sobre la base `aria-live` que ya tenía); `scope="col"` en los encabezados de la tabla de uso por agente (dejó anotado que `clientes/page.tsx` tiene el mismo hueco — no corregido ahí, backport pendiente no bloqueante). **Alcance reducido a propósito, ya anotado en la casilla de la tarea**: "tiempo real (WebSocket)" del enunciado original NO se construyó — no hay infraestructura de tiempo real en el proyecto (ni servidor WS propio ni Supabase Realtime configurado/auditado); la pantalla se actualiza con `router.refresh()` al guardar el límite o al navegar, no en vivo. Decisión deliberada por tamaño/riesgo (un canal de datos nuevo necesitaría su propia auditoría de seguridad) más que un descuido — queda como tarea propia a futuro, con Supabase Realtime (viene con el stack) como candidato natural sobre `registros_uso`/`uso_diario` filtrado por RLS. Verificado por mí al final: `typecheck` limpio (caché borrada), `npm run test` 91/91 en verde (76 previos + 15 nuevos de `db-guardian` en `app/tests/rls/07-limite-mensual-cliente.test.ts`, incluyendo un test "canario" que fija la lista exacta de políticas no-SELECT de `clientes` en `["clientes_operador"]` para que una futura política de UPDATE agregada sin querer haga fallar un test), `GET /portal/uso` 200 por curl con la cuenta `cliente@kaudal.local`. **Fase 7 completa: DoD cumplida** (7.1 endpoint de uso, 7.2 calculadora, 7.3 el cliente ve su uso y su límite con aviso — sin hallazgos altos abiertos en ninguna de las tres).
- 2026-08-27 · 8.1 · Inicio del cliente + "Mi agente" (docs/eng/06 §4/§5), construido por mí directamente. Antes de esta tarea `/portal` era el placeholder fijo que dejó 3.2 ("tu portal se está construyendo..."), y el topbar del portal (`empresaNombre`/`costoEstimadoClp`, docs/eng/06 §2: "siempre visibles") nunca se alimentaba desde ninguna pantalla real — se veía "Tu empresa" sin costo incluso en `/portal/uso` y `/portal/cuenta`, ya construidas en fases previas. Nuevo: `lib/portal/contexto.ts` (`obtenerContextoPortal` — nombre de empresa + costo del mes, centralizado para no repetir la query en cada pantalla), `lib/portal/agentes-cliente.ts` (`obtenerAgentesConUsoDelMes` — agentes vía `agentes_publicos` con uso/costo del mes agregado desde `uso_diario` y última actividad resuelta por agente desde `registros_uso.ocurrido_en`; sin `GROUP BY` porque Supabase-JS no lo expone, aceptable porque un cliente tiene pocos agentes), `lib/agentes/estado-cliente.ts` (`calcularEstadoCliente`, función pura y testeable — traduce el vocabulario interno `activo/pausado/caido` al vocabulario de 3 estados del cliente: `caido`→Con problemas, `pausado`→Sin uso reciente (es una acción del operador, no una falla, así que no alarma), `activo` con actividad dentro de 48h→Funcionando, si no Sin uso reciente), `lib/formato/tiempo-relativo.ts` ("Hace 12 min"/"Hace 3 h"), `components/ui/AgenteClienteEstadoChip.tsx` y `components/ui/ClientAgentCard.tsx` (deliberadamente NO reutilizan `AgenteEstadoChip`/`AgentCard` del operador: mostrar el vocabulario interno o el badge de `tipo` mastra/n8n/propio rompería la regla de "caja negra: sin exponer prompts/lógica"). Reescribí `portal/page.tsx` (Inicio: 4 KPIs, gráfico de uso por día del mes — usa el mismo período de mes calendario que "Uso y costo" en vez de los "últimos 30 días" literales del doc, para no tener dos ventanas de tiempo distintas en pantallas contiguas; sección "Mi agente" con tarjetas; franja de contacto), agregué `portal/agentes/page.tsx` (lista completa "Mis agentes") y `portal/agentes/[id]/page.tsx` (detalle: uso, costo, "dónde vive" — deriva de `estado`+`canal`, nunca de `endpoint_url`/`health_url`; el botón "¿Tienes una duda sobre este agente?" del doc no se construyó, es tarea 9.1). Moví `UsoPorDia.tsx` a `components/portal/` (antes vivía dentro de `app/portal/uso/`) para reutilizarlo en 3 pantallas sin duplicar el gráfico. Aproveché para cerrar un hueco que no era de esta tarea pero bloqueaba el objetivo real de "portal consistente": pasé `empresaNombre`/`costoEstimadoClp` también a `portal/uso/page.tsx` y `portal/cuenta/page.tsx` (antes solo mostraban el fallback). Como consecuencia tuve que reubicar `CerrarSesionBoton` — vivía solo en el placeholder de Inicio que acabo de reemplazar, y no estaba en ningún otro lado (ni el operador tiene cerrar sesión todavía, deuda ya conocida de 1.3); lo moví a Mi cuenta, que es donde docs/eng/06 §8 lo pone explícitamente ("Cerrar sesión" bajo Mi cuenta), así que quedó mejor ubicado que antes, no solo desplazado. Verifiqué todo con datos reales, no solo con la base vacía: inserté un fixture temporal por SQL directo (3 agentes en los 3 estados posibles, 41 registros de uso, 1 ticket abierto) contra el cliente semilla, confirmé por curl que los 4 KPIs, el conteo "1 de 3 funcionando", el costo `≈ $1.215` (40×$30 + 1×$15, exacto) y los 3 chips de estado (Funcionando/Sin uso reciente/Con problemas) calzan con lo esperado, y limpié el fixture al terminar (no toca `audit_log`, no hizo falta `db reset`). `diseno-moderno` encontró y corrigió 3 problemas reales: el costo estimado en `portal/uso/page.tsx` y en `ClientAgentCard` se mostraba SIN el prefijo "≈" (regla de marca explícita: "el costo SIEMPRE con ≈"), las etiquetas de `ClientAgentCard` no coincidían textualmente con las de `StatCard` en otras pantallas, y la fila de 4 KPIs de Inicio pasaba a una sola fila recién en `lg:` mientras el resto del portal lo hace desde `sm:` (dejaba Inicio en 2×2 en tablets mientras todo lo demás ya iba en una fila) — corregido a `sm:grid-cols-4`. También marcó, sin tocarlo (no es de esta tarea), que `ClientCard.tsx` del lado OPERADOR tiene el mismo bug del "≈" faltante. `accesibilidad` (bloqueante) encontró y corrigió: contraste insuficiente en las etiquetas de fecha de `UsoPorDia` (`text-text-faint` ~3.98:1, bajo AA — cambiado a `text-text-muted` ~8.3:1; señaló que el mismo token afecta otras pantallas no tocadas hoy, deuda aparte); 3 links ("Ver todos →", "Cuéntanos →", "← Mis agentes") sin `focus-visible` ni el mínimo de 44px de toque, corregidos; y `CerrarSesionBoton` usando `size="sm"` (36px) pese a que el propio comentario de `Button.tsx` prohíbe ese tamaño para "única acción táctil en móvil/portal cliente" — exactamente este caso, corregido al tamaño `md` por defecto. Encontró además un hallazgo que no pudo corregir por no tener la herramienta `Write`: no existía ningún `not-found.tsx` bajo `app/`, así que el `notFound()` de `portal/agentes/[id]/page.tsx` (cuando el id no existe o no es del cliente — RLS ya lo filtra, se trata igual sin distinguir para no filtrar existencia de IDs ajenos) caía en el 404 genérico de Next.js, sin `AppShell` ni forma de volver. Lo creé yo: `portal/not-found.tsx` (mismo patrón de sesión/rol que el resto del portal, botón "Volver a Inicio"), verificado por curl (404 real con la UI propia, no la de Next — confirmé leyendo el árbol RSC completo, no solo un grep de texto, porque el payload de hidratación incluye un fallback global de Next que nunca se monta y un grep ingenuo lo habría contado como un falso positivo). Verificado por mí al final, con todos los cambios ya aplicados: `typecheck` limpio (caché borrada), `npm run test` 91/91 en verde, y una ronda completa de curl (200 en las 4 pantallas del portal, 404 propio para un agente inexistente). **Fase 8 aún no completa** (falta 8.2, microcopy y estados vacíos — gran parte ya quedó cubierta como consecuencia directa de 8.1: estados vacíos de Inicio/Mis agentes con título+bajada+CTA siguiendo el patrón ya establecido, cero jerga técnica verificado explícitamente por `diseno-moderno`/`accesibilidad` en esta misma pasada).
- 2026-08-27 · 8.2 · Microcopy y estados vacíos (docs/eng/06 §9, "biblioteca rápida"). La mayor parte de la tabla ya quedó resuelta como efecto directo de 8.1 (estados vacíos con título+bajada+CTA, "≈"+estimado en todo costo, cero jerga verificado por `diseno-moderno`/`accesibilidad`) o de fases anteriores ("Guardado ✅" ya en los toasts de `LimiteMensual`/`ReemplazarApiKey`, confirmación de acción destructiva ya en `DesconectarApiKey` — de hecho más específica que el "¿Seguro?" genérico del doc, porque `compliance-cl` en 5.1 pidió explicar que desconectar en Kaudal no revoca la key en el proveedor). Lo que faltaba de verdad, sin dueño en ningún archivo: 3 situaciones de la tabla no tenían NINGUNA implementación en todo el proyecto. Agregué las 3, alcance acotado a exactamente lo que pide la tabla, nada más: (1) `app/portal/loading.tsx` ("Buscando tus usos…") — antes de esto, ninguna ruta de la app tenía un `loading.tsx`; una consulta lenta a Supabase dejaba la pantalla en blanco sin ningún aviso mientras el Server Component resolvía. Reutiliza el prop `loading` de `StatCard` (existía desde la 1.2, nunca se había usado en la app real) para las 4 tarjetas KPI. (2) `app/portal/error.tsx` ("No pudimos cargar esto. Reintenta en un momento." + botón Reintentar) — antes de esto cualquier excepción no capturada en una pantalla del portal caía en el error boundary genérico de Next.js (en inglés, sin estilo, sin `AppShell`). Ninguno de los dos recibe `empresaNombre`/`costoEstimadoClp` en el topbar: son las dos únicas pantallas del portal que no pueden esperar a que la sesión/datos resuelvan (ese es justamente el problema que resuelven), así que el topbar muestra el fallback "Tu empresa" un instante — deliberado, no un olvido. (3) `components/system/AvisoSinConexion.tsx` ("Parece que te quedaste sin conexión. Revisa tu internet.") — no existía ningún listener de `window.addEventListener("offline", ...)` en toda la app; lo monté una sola vez en `layout.tsx` (dentro de `ToastProvider`, como hermano de `children`) porque perder la conexión no es un concepto exclusivo del portal cliente — el operador lo necesita igual. No agregué un aviso de "conexión restablecida": el doc no lo pide y no quise inventar copy que nadie definió. Verificado: `typecheck` limpio (caché borrada), `npm run test` 91/91 en verde, y por curl con ambas sesiones reales (`cliente@kaudal.local` y `operador@kaudal.local`) que ninguna pantalla de ningún rol se rompió con el cambio a `layout.tsx` (root layout, afecta a toda la app). Grep explícito de "token"/"endpoint"/"payload" sobre todo `app/portal` y `components/portal`: cero coincidencias. **Fase 8 completa: DoD cumplida** (portal bonito y entendible: Inicio, Mis agentes, detalle de agente, Uso y costo y Mi cuenta consistentes entre sí, con estados de carga/error/sin conexión/vacíos que enseñan en vez de romperse en blanco).
- 2026-08-29 · 6.3 · Auditoría transversal terminada: triggers `SECURITY DEFINER` para altas y cambios de ciclo de vida de clientes, usuarios, agentes, suscripciones y cobros; API keys y tickets conservan sus auditorías propias. No se registran secretos, material criptográfico ni endpoints. Agregados 3 tests y ajustada la limpieza de fixtures HTTP para eliminar solo su bitácora de prueba. Verificado con `typecheck` y 140/140 tests verdes.
- 2026-08-29 · 9.1 · Revisada y cerrada la implementación existente de tickets del cliente: alta de duda/reclamo, primer mensaje y hasta 5 adjuntos privados; formulario, listado y detalle del portal; hilo y respuestas; URLs firmadas de 5 min. La BD aplica RLS, límite de 10 tickets/hora, estados, lectura y auditoría; Storage separa adjuntos públicos de notas internas. Verificado con `typecheck` y 140/140 tests verdes, incluidas regresiones de body multipart sin `Content-Length`, aislamiento por org/cliente y adjuntos internos.
- 2026-08-30 · 11.5.4 · Chequeo de rentabilidad implementado en la bandeja de instancias: muestra margen real y mínimo exigido; función pura cubierta por tests. El webhook sandbox valida sus escrituras y devuelve error si no logra sincronizar una instancia.
- 2026-08-30 · 11.5.2 · Formulario de registro manual agregado a Instancias; permite asociar cliente, suscripción, proveedor, URL y costo. La activación pasa por el trigger de BD que exige suscripción activa con cobertura; typecheck y pruebas de gating verdes.
